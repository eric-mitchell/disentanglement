train:
  default: yes
  flags-import: all
  flags-import-skip: [device]
  output-scalars:
    '^(\key) := (\value)'

inspect:
  main: inspect_weights
  flags-import: all

train_comp:
  flags-import: all
  output-scalars:
    '^(\key) := (\value)'
